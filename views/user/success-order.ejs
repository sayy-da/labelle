<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Confirmed — Labelle</title>
  <link rel="icon" href="/img/core-img/favicon.ico">
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,400&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
  <style>
    *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

    :root {
      --cream: #f9f4ee;
      --warm: #fdf9f5;
      --gold: #c9a96e;
      --gold-lt: #e8d5b0;
      --dark: #1a1410;
      --muted: #8a7968;
      --border: rgba(201,169,110,0.2);
      --green: #2e7d32;
    }

    body {
      font-family: 'DM Sans', sans-serif;
      background: #f4efe9;
      color: var(--dark);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* ── NAV ── */
    nav {
      height: 64px;
      background: rgba(253,249,245,0.97);
      border-bottom: 1px solid var(--border);
      display: flex; align-items: center; justify-content: space-between;
      padding: 0 5vw;
    }
    .logo {
      font-family: 'Cormorant Garamond', serif;
      font-size: 1.6rem; font-weight: 600; letter-spacing: 0.1em;
      color: var(--dark); text-decoration: none;
    }
    .logo span { color: var(--gold); }
    .nav-links { display: flex; gap: 2rem; }
    .nav-links a {
      font-size: 0.75rem; letter-spacing: 0.12em; text-transform: uppercase;
      color: var(--muted); text-decoration: none; transition: color 0.2s;
    }
    .nav-links a:hover { color: var(--dark); }

    /* ── PROGRESS ── */
    .progress-wrap {
      background: #fff;
      border-bottom: 1px solid var(--border);
      padding: 1.2rem 5vw;
      display: flex; align-items: center;
    }
    .prog-step {
      display: flex; align-items: center; gap: 0.6rem;
      font-size: 0.7rem; letter-spacing: 0.1em; text-transform: uppercase;
    }
    .prog-num {
      width: 26px; height: 26px; border-radius: 50%;
      display: flex; align-items: center; justify-content: center;
      font-size: 0.65rem; font-weight: 500; flex-shrink: 0;
    }
    .prog-step.done .prog-num { background: var(--gold); color: #fff; }
    .prog-step.active .prog-num { background: var(--dark); color: var(--cream); }
    .prog-step .prog-num { background: var(--border); color: var(--muted); }
    .prog-label { color: var(--muted); }
    .prog-step.active .prog-label { color: var(--dark); font-weight: 500; }
    .prog-line { flex: 1; height: 1px; background: var(--border); margin: 0 1rem; max-width: 60px; }

    /* ── MAIN ── */
    .main {
      flex: 1;
      display: flex; align-items: center; justify-content: center;
      padding: 4rem 5vw;
    }

    .success-card {
      background: #fff;
      border: 1px solid var(--border);
      border-radius: 4px;
      max-width: 520px;
      width: 100%;
      overflow: hidden;
      text-align: center;
      box-shadow: 0 8px 40px rgba(26,20,16,0.08);
      animation: fadeUp 0.6s ease both;
    }

    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(24px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    /* top gold bar */
    .success-top {
      background: var(--dark);
      padding: 2.5rem 2rem 2rem;
      position: relative;
      overflow: hidden;
    }
    .success-top::before {
      content: '';
      position: absolute; top: -40%; right: -10%;
      width: 200px; height: 200px;
      background: radial-gradient(circle, rgba(201,169,110,0.15) 0%, transparent 70%);
      pointer-events: none;
    }

    /* ── CHECKMARK ANIMATION ── */
    .check-wrap {
      width: 90px; height: 90px;
      margin: 0 auto 1.2rem;
      position: relative;
    }

    @keyframes circle-draw {
      from { stroke-dashoffset: 283px; }
      to   { stroke-dashoffset: 0; }
    }
    @keyframes circle-fill {
      0%   { opacity: 0; }
      60%  { opacity: 0; }
      100% { opacity: 1; }
    }
    @keyframes check-draw {
      from { stroke-dashoffset: 80px; }
      to   { stroke-dashoffset: 0; }
    }

    .check-wrap svg .outer-circle {
      stroke-dasharray: 283px;
      stroke-dashoffset: 283px;
      animation: circle-draw 0.7s cubic-bezier(0.4,0,0.2,1) 0.1s forwards;
    }
    .check-wrap svg .fill-circle {
      opacity: 0;
      animation: circle-fill 0.9s ease 0.1s forwards;
    }
    .check-wrap svg .checkmark {
      stroke-dasharray: 80px;
      stroke-dashoffset: 80px;
      animation: check-draw 0.4s ease 0.75s forwards;
    }

    .success-top-eyebrow {
      font-size: 0.62rem; letter-spacing: 0.3em; text-transform: uppercase;
      color: var(--gold); margin-bottom: 0.4rem;
    }
    .success-top h1 {
      font-family: 'Cormorant Garamond', serif;
      font-size: 2rem; font-weight: 300; color: var(--cream); line-height: 1.2;
    }
    .success-top h1 em { font-style: italic; color: var(--gold-lt); }

    /* body */
    .success-body {
      padding: 2rem;
    }

    .success-message {
      font-size: 0.88rem; color: var(--muted);
      line-height: 1.7; margin-bottom: 1.8rem;
    }

    /* info tiles */
    .info-grid {
      display: grid; grid-template-columns: 1fr 1fr;
      gap: 0.8rem; margin-bottom: 1.8rem;
    }
    .info-tile {
      background: var(--cream);
      border: 1px solid var(--border);
      border-radius: 4px;
      padding: 0.9rem 1rem;
      text-align: left;
    }
    .info-tile-label {
      font-size: 0.58rem; letter-spacing: 0.18em; text-transform: uppercase;
      color: var(--muted); margin-bottom: 0.3rem;
    }
    .info-tile-value {
      font-size: 0.85rem; color: var(--dark); font-weight: 500;
    }
    .info-tile-value.gold { color: var(--gold); }

    /* divider */
    .divider {
      height: 1px; background: var(--border); margin: 0 0 1.8rem;
    }

    /* buttons */
    .btn-primary {
      display: block; width: 100%; padding: 0.95rem;
      background: var(--gold); color: var(--dark);
      border: none; border-radius: 2px;
      font-family: 'DM Sans', sans-serif;
      font-size: 0.75rem; letter-spacing: 0.18em; text-transform: uppercase; font-weight: 500;
      cursor: pointer; text-decoration: none;
      transition: all 0.2s; margin-bottom: 0.7rem;
    }
    .btn-primary:hover { background: var(--dark); color: var(--cream); transform: translateY(-1px); }

    .btn-secondary {
      display: block; width: 100%; padding: 0.9rem;
      background: transparent; color: var(--muted);
      border: 1px solid var(--border); border-radius: 2px;
      font-family: 'DM Sans', sans-serif;
      font-size: 0.72rem; letter-spacing: 0.15em; text-transform: uppercase;
      cursor: pointer; text-decoration: none;
      transition: all 0.2s;
    }
    .btn-secondary:hover { border-color: var(--gold); color: var(--gold); }

    /* ── FOOTER ── */
    footer {
      background: #130e09; padding: 2rem 5vw;
      border-top: 1px solid rgba(201,169,110,0.1);
      display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem;
    }
    .footer-logo { font-family: 'Cormorant Garamond', serif; font-size: 1.3rem; font-weight: 600; letter-spacing: 0.1em; color: var(--cream); }
    .footer-logo span { color: var(--gold); }
    .footer-copy { font-size: 0.7rem; color: rgba(249,244,238,0.2); }
    .footer-legal { display: flex; gap: 1.4rem; }
    .footer-legal a { font-size: 0.7rem; color: rgba(249,244,238,0.2); text-decoration: none; transition: color 0.2s; }
    .footer-legal a:hover { color: var(--gold); }
  </style>
</head>
<body>

<!-- NAV -->
<nav>
  <a href="/" class="logo">La<span>belle</span></a>
  <div class="nav-links">
    <a href="/">Home</a>
    <a href="/shop">Shop</a>
    <a href="/user-profile">Profile</a>
  </div>
</nav>

<!-- PROGRESS -->
<div class="progress-wrap">
  <div class="prog-step done">
    <div class="prog-num"><i class="fa fa-check" style="font-size:0.5rem;"></i></div>
    <span class="prog-label">Cart</span>
  </div>
  <div class="prog-line"></div>
  <div class="prog-step done">
    <div class="prog-num"><i class="fa fa-check" style="font-size:0.5rem;"></i></div>
    <span class="prog-label">Checkout</span>
  </div>
  <div class="prog-line"></div>
  <div class="prog-step active">
    <div class="prog-num"><i class="fa fa-check" style="font-size:0.5rem;"></i></div>
    <span class="prog-label">Confirmed</span>
  </div>
</div>

<!-- MAIN -->
<div class="main">
  <div class="success-card">

    <!-- Top dark section with animation -->
    <div class="success-top">
      <div class="check-wrap">
        <svg viewBox="0 0 90 90" fill="none" xmlns="http://www.w3.org/2000/svg" width="90" height="90">
          <!-- Gold fill circle -->
          <circle class="fill-circle" cx="45" cy="45" r="40" fill="#c9a96e"/>
          <!-- Animated border circle -->
          <circle class="outer-circle" cx="45" cy="45" r="40" stroke="#c9a96e" stroke-width="2" fill="none"/>
          <!-- Checkmark -->
          <polyline class="checkmark" points="26,46 38,58 64,32" stroke="#fff" stroke-width="5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </div>
      <p class="success-top-eyebrow">Order Confirmed</p>
      <h1>Thank you for your <em>purchase</em></h1>
    </div>

    <!-- Body -->
    <div class="success-body">
      <p class="success-message">
        Your order has been placed successfully. We'll send you a confirmation shortly and notify you when it ships.
      </p>

      <div class="info-grid">
        <div class="info-tile">
          <div class="info-tile-label">Status</div>
          <div class="info-tile-value gold">Processing</div>
        </div>
        <div class="info-tile">
          <div class="info-tile-label">Delivery</div>
          <div class="info-tile-value">3–5 Business Days</div>
        </div>
        <div class="info-tile" style="grid-column: 1 / -1;">
          <div class="info-tile-label">Shipping</div>
          <div class="info-tile-value" style="color: var(--green);">Free Shipping</div>
        </div>
      </div>

      <div class="divider"></div>

      <a href="/user-order-details/<%= orderId %>" class="btn-primary">
        View My Order &nbsp;<i class="fa fa-arrow-right" style="font-size:0.7rem;"></i>
      </a>
      <a href="/shop" class="btn-secondary">
        Continue Shopping
      </a>
    </div>

  </div>
</div>

<!-- FOOTER -->
<footer>
  <div class="footer-logo">La<span>belle</span></div>
  <p class="footer-copy">© 2025 Labelle. All rights reserved.</p>
  <div class="footer-legal">
    <a href="#">Privacy Policy</a>
    <a href="#">Terms</a>
  </div>
</footer>

</body>
</html>