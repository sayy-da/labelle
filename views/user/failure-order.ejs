<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Labelle</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      margin: 0;
      padding: 20px;
    }

    .animation-ctn {
      text-align: center;
      margin: 5em auto;
    }

    .icon--order-failure {
      animation: appear 0.5s ease-in-out;
    }

    .icon--order-failure circle {
      animation: circle-draw 1s ease-in-out forwards;
    }

    .icon--order-failure line:nth-child(2) {
      animation: cross-draw 0.5s ease-in-out 0.5s forwards;
    }

    .icon--order-failure line:nth-child(3) {
      animation: cross-draw 0.5s ease-in-out 0.7s forwards;
    }

    @keyframes appear {
      0% { opacity: 0; }
      100% { opacity: 1; }
    }

    @keyframes circle-draw {
      0% { stroke-dashoffset: 480px; }
      100% { stroke-dashoffset: 0; }
    }

    @keyframes cross-draw {
      0% { stroke-dashoffset: 100px; }
      100% { stroke-dashoffset: 0; }
    }

    h1 {
      color: #333;
      margin: 30px 0;
      font-size: 24px;
    }

    .btn {
      background-color: #fbb710;
      color: white;
      border: none;
      padding: 12px 24px;
      border-radius: 8px;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      transition: all 0.3s ease;
      text-decoration: none;
      display: inline-block;
    }

    .btn:hover {
      background-color: #e5a60f;
      transform: translateY(-2px);
      box-shadow: 0 6px 8px rgba(0, 0, 0, 0.15);
    }
  </style>
</head>
<body>
  <div class="animation-ctn">
    <div class="icon icon--order-failure">
      <svg xmlns="http://www.w3.org/2000/svg" width="154px" height="154px">
        <g fill="none" stroke="#FF0000" stroke-width="2">
          <circle cx="77" cy="77" r="72" style="stroke-dasharray: 480px, 480px;"></circle>
          <line x1="50" y1="50" x2="104" y2="104" stroke="#FF0000" stroke-width="10" style="stroke-dasharray: 100px, 100px;"></line>
          <line x1="104" y1="50" x2="50" y2="104" stroke="#FF0000" stroke-width="10" style="stroke-dasharray: 100px, 100px;"></line>
        </g>
      </svg>
    </div>
    <h1 style="margin-bottom: 0px;">We are unable to process your order this time</h1>
    <h4 style="color: #868282;">You have 24 hours to complete the payment. After that, the order will be automatically canceled.</h4>
    <p style="color: #ada2a2;">See Orders to retry your payment</p>
    <a href="/user-order-details/<%= orderId %>" class="btn">
      View Orders →
    </a>
  </div>
</body>
</html>